<p id="notice"><%= notice %></p>

<h3>Assignments</h3>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% @submissions.each do |submission| %>
    <div class='d-flex flex-row'>
      <span class='nav-link btn' role='button'>Download<span>
      <iframe src="<%= (url_for(submission.solution)) %>" />
    </div>
  <% end %>
  </tbody>
</table>

<br>

