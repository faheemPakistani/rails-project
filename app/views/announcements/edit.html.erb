<h1> Edit Announcement</h1>
<%= form_for([@classroom, @announcement]) do |f| %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, required: true%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', classrooms_path %>