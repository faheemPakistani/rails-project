<%= form_with model: [classroom, classroom.announcements.build ] do |form| %>
  <p class='bg-white p-3 card shadow-lg mb-5 bg-white rounded'>
      <%= form.rich_text_area :body,  required: true%>
  </p>
  <p>
      <%= form.submit :class => "btn btn-primary mb-2 classroom_read_button"%>
  </p>
<% end %>
