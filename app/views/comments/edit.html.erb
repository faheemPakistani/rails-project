<h1> Edit Comments</h1>
<%= form_for([@post, @comment]) do |f| %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, required: true%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', posts_path %>