<h1> Edit suggestion</h1>
<%= form_for([@post, @suggestion]) do |f| %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, required: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', posts_path %>